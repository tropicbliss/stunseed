[package]
name = "benchmark"
version = "0.1.0"
edition = "2021"

[dependencies]
stunseed = { version = "0.1.0", path = "../stunseed" }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "teams"
harness = false
